可以，而且**这是一个非常好的“元提示（Meta-Prompt）”使用场景**。
下面这份是我整理的 **「给其他 AI 用的 noscript 生成指南」**，目标是：

> **稳定命中 Bing 抓取 + 关键词可控 + 不触发 cloaking**

你可以直接把它当成 **系统提示 / 开发者提示 / 生成规范** 给任何 AI 用（ChatGPT、Claude、Gemini 都适用）。

---

# ✅ Noscript 内容生成指南（AI 专用）

## 一、总体目标（必须遵守）

生成的 `<noscript>` 内容应满足以下四点：

1. **无需 JavaScript 即可完整理解页面主题**
2. **与 JS 渲染页面语义一致**
3. **可被 Bing 在首轮 HTML 抓取中完整解析**
4. **自然命中目标关键词而不堆砌**

---

## 二、生成范围与禁止事项

### ✔ 允许内容

* 页面主题介绍
* 产品 / 服务的功能概述
* 使用场景说明
* 页面内部导航链接
* 权威参考链接（可选）

### ❌ 禁止内容

* 虚假承诺（如 free、pricing、instant access 若 JS 页未提供）
* API Key、Endpoint、使用教程细节
* 关键词堆砌
* 与 JS 页面主题不一致的营销话术

---

## 三、结构规范（强制）

生成的 `<noscript>` **必须包含以下结构**：

```html
<noscript>
  <section class="noscript-content">
    <h1>...</h1>
    <p>...</p>
    <h2>...</h2>
    <p>...</p>
    <ul>
      <li>...</li>
    </ul>
    <h2>...</h2>
    <ul>
      <li><a href="...">...</a></li>
    </ul>
  </section>
</noscript>
```

---

## 四、标题生成规则（非常重要）

### H1（必须且唯一）

* 包含 **品牌名**
* 包含 **核心关键词**
* 不重复

**示例：**

> Pomeli AI API Aggregation Platform

---

### H2（2–4 个）

* 功能型关键词
* 场景型关键词
* 语义清晰

**示例：**

* Unified AI API Interface
* Gemini-Compatible API Routing
* Developer-Focused AI Integration

---

## 五、正文生成规则（关键词命中）

### 1️⃣ 核心关键词

* 出现 **1–2 次**
* 放在：

  * H1
  * 首段正文

### 2️⃣ 相关关键词 / 同义词

* 自然出现
* 分散在段落中
* 不集中重复

### 3️⃣ 关键词密度上限

* **≤ 2.5%**
* 严禁连续重复

---

## 六、正文长度要求（Bing 甜点区）

| 类型   | 要求                |
| ---- | ----------------- |
| 最小长度 | 150 English words |
| 推荐长度 | 250–500 words     |
| 最大长度 | 800 words         |

---

## 七、链接生成规范（加权项）

### 内部链接（必须）

* 3–10 个
* 指向站内真实存在页面
* 使用描述性 anchor text

**示例：**

```html
<a href="/models">Supported AI Models</a>
```

---

### 外部链接（可选）

* 0–2 个
* 指向权威站点
* 不带 tracking 参数

---

## 八、语义一致性校验（必须通过）

生成完成后，AI 必须自检：

* [ ] noscript 内容是否是 JS 页面语义的 **摘要版**
* [ ] 是否存在 JS 页面未提及的功能
* [ ] 是否使用了误导性营销词
* [ ] 是否存在关键词堆砌

**如有任何一项不通过，必须重写。**

---

## 九、HTML 技术要求（硬性）

* 放置在 `<body>` 内，靠前位置
* 不使用 `display:none`
* 使用语义化 HTML（h1 h2 p ul li a）
* 不插入 script、style、iframe

---

## 十、最终输出格式（强制）

AI **只输出** `<noscript>` 内完整 HTML
不附带解释、不加注释、不输出多余文本

---

## 十一、总结一句话（给 AI 的底层指令）

> **你正在为 Bing 生成“无需 JS 的真实页面版本”，而不是 SEO 文本。**

---

