# Favicon 配置指南

## 必需文件（放在 `public/` 目录）

```
public/
  ├── favicon.ico           (16x16, 32x32, 48x48 多尺寸 ICO)
  ├── favicon.svg           (SVG 矢量图)
  ├── favicon-96x96.png     (96x96 像素 - Bing 推荐)
  ├── favicon-32x32.png     (32x32 像素)
  ├── favicon-16x16.png     (16x16 像素)
  └── apple-touch-icon.png  (180x180 像素 - Apple 设备)
```

## 文件尺寸要求

| 文件 | 尺寸 | 用途 |
|------|------|------|
| `favicon.ico` | 16x16, 32x32, 48x48 | 传统浏览器兼容 |
| `favicon.svg` | 矢量 | 现代浏览器，高分辨率 |
| `favicon-96x96.png` | 96x96 | Bing 搜索结果 |
| `favicon-32x32.png` | 32x32 | 标准浏览器标签页 |
| `favicon-16x16.png` | 16x16 | 最小尺寸 |
| `apple-touch-icon.png` | 180x180 | iOS 设备主屏幕 |

## 配置代码

已在 `src/app/layout.tsx` 中配置：

```typescript
export const metadata: Metadata = {
  icons: {
    icon: [
      { url: "/favicon.ico", sizes: "any" },
      { url: "/favicon.svg", type: "image/svg+xml" },
      { url: "/favicon-96x96.png", sizes: "96x96", type: "image/png" },
      { url: "/favicon-32x32.png", sizes: "32x32", type: "image/png" },
      { url: "/favicon-16x16.png", sizes: "16x16", type: "image/png" },
    ],
    apple: [
      { url: "/apple-touch-icon.png", sizes: "180x180", type: "image/png" },
    ],
    shortcut: "/favicon.ico",
  },
};
```

## 生成文件方法

### 在线工具
- https://realfavicongenerator.net/
- https://favicon.io/

### 命令行（ImageMagick）
```bash
convert logo.png -resize 16x16 favicon-16x16.png
convert logo.png -resize 32x32 favicon-32x32.png
convert logo.png -resize 96x96 favicon-96x96.png
convert logo.png -resize 180x180 apple-touch-icon.png
convert logo.png -define icon:auto-resize=16,32,48 favicon.ico
```

## 搜索引擎要求

### Google
- 至少提供 `favicon.ico` 或 `favicon.png`
- 推荐尺寸：32x32 或更大
- 文件大小：< 100KB

### Bing
- 必须提供 `favicon.ico`
- 推荐尺寸：96x96 像素
- 必须包含 `shortcut` 图标（已配置）

## 验证

1. 检查文件可访问：
   - `https://yourdomain.com/favicon.ico`
   - `https://yourdomain.com/favicon-96x96.png`

2. 检查 HTML 源码中的 `<head>` 标签

3. 使用 Google Search Console 和 Bing Webmaster Tools 验证

## 注意事项

- 确保 `robots.txt` 不阻止 favicon 文件
- 文件命名必须与配置中的 URL 完全匹配
- 文件大小优化：ICO < 100KB, PNG < 50KB, SVG < 20KB

