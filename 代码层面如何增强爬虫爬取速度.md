# 代码层面如何增强爬虫爬取速度

基于 seedance2.us 分支的深度研究，以下是所有可落地的代码级优化点。

---

## 一、明确告知爬虫抓取策略（BaseLayout.astro）

grokapi.org 没有这几行，爬虫只能靠默认行为猜测。

```html
<!-- 告诉所有爬虫：可以索引、可以跟踪链接、图片预览不限制、摘要不截断 -->
<meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1">

<!-- 针对 Google 单独声明 -->
<meta name="googlebot" content="index, follow">

<!-- 针对 Bing 单独声明 -->
<meta name="bingbot" content="index, follow">
```

`max-snippet:-1` 让 Google 在搜索结果里显示更长的摘要，提升点击率。
`max-image-preview:large` 允许 Google 展示大图预览，对图片类内容尤其重要。

---

## 二、DNS 预解析 + 预连接（BaseLayout.astro）

爬虫抓取页面时也会加载外部资源，提前解析 DNS 可以减少等待时间，让页面更快完成加载。

```html
<!-- DNS 预解析（低成本，全部加上） -->
<link rel="dns-prefetch" href="https://fonts.googleapis.com">
<link rel="dns-prefetch" href="https://fonts.gstatic.com">
<link rel="dns-prefetch" href="https://www.clarity.ms">

<!-- 预连接（建立 TCP+TLS，比 dns-prefetch 更彻底） -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
```

注意：`dns-prefetch` 和 `preconnect` 可以同时写，浏览器/爬虫会选择支持的那个。

---

## 三、HTTP 缓存头策略（public/_headers）

爬虫重复抓取时，合理的缓存头可以让爬虫判断资源是否需要重新下载，减少带宽消耗，提升 crawl budget 利用率。

```
# HTML 不缓存，保证爬虫每次都拿最新内容
/*.html
  Cache-Control: public, max-age=0, must-revalidate

# JS/CSS/图片/字体 长期缓存（内容不变则不重复抓取）
/*.js
  Cache-Control: public, max-age=31536000, immutable
/*.css
  Cache-Control: public, max-age=31536000, immutable
/*.png
  Cache-Control: public, max-age=31536000, immutable
/*.webp
  Cache-Control: public, max-age=31536000, immutable
/*.woff2
  Cache-Control: public, max-age=31536000, immutable

# sitemap 和 robots.txt 短期缓存（1小时），更新后爬虫能及时感知
/sitemap.xml
  Cache-Control: public, max-age=3600
/robots.txt
  Cache-Control: public, max-age=3600
```

---

## 四、robots.txt 明确声明 sitemap 位置

```
User-agent: *
Allow: /

Sitemap: https://yourdomain.com/sitemap.xml
```

Bing 和 Google 都会主动读取 robots.txt 里的 Sitemap 字段，这是让爬虫发现所有页面最直接的方式。

---

## 五、JSON-LD 结构化数据增强（BaseLayout.astro）

### 5.1 补全多语言的 inLanguage 映射

grokapi.org 只处理了 zh/zh-tw/en，其他语言都 fallback 到 en-US，会导致结构化数据语言标注不准确。

```js
// 完整映射
"inLanguage": locale === 'zh-tw' ? 'zh-TW' 
  : locale === 'zh' ? 'zh-CN' 
  : locale === 'ja' ? 'ja-JP' 
  : locale === 'ko' ? 'ko-KR' 
  : locale === 'hi' ? 'hi-IN' 
  : locale === 'es' ? 'es-ES' 
  : locale === 'pt' ? 'pt-PT' 
  : locale === 'ru' ? 'ru-RU' 
  : locale === 'de' ? 'de-DE' 
  : locale === 'fr' ? 'fr-FR' 
  : 'en-US'
```

### 5.2 WebSite 节点补充 publisher 信息

```js
{
  "@type": "WebSite",
  "name": "站点名",
  "url": BASE_URL,
  "publisher": {
    "@type": "Organization",
    "name": "站点名",
    "url": BASE_URL
  }
}
```

### 5.3 WebPage 节点补充 breadcrumb

面包屑结构化数据让 Google 在搜索结果里显示路径，提升点击率，同时帮助爬虫理解站点层级。

```js
{
  "@type": "WebPage",
  "breadcrumb": {
    "@type": "BreadcrumbList",
    "itemListElement": [
      {
        "@type": "ListItem",
        "position": 1,
        "name": "Home",
        "item": BASE_URL
      }
    ]
  }
}
```

---

## 六、OG / Twitter 标签补全（BaseLayout.astro）

```html
<!-- 图片 alt，帮助爬虫理解图片内容 -->
<meta property="og:image:alt" content="{title} - 站点描述">
<meta name="twitter:image:alt" content="{title} - 站点描述">

<!-- 站点名，让 Google 在搜索结果里显示品牌名 -->
<meta property="og:site_name" content="站点名">

<!-- Twitter 用 name 而不是 property -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content={title}>
<meta name="twitter:description" content={description}>
```

注意：Twitter 的 meta 应该用 `name=` 而不是 `property=`，grokapi.org 用的是 `property=`，虽然大多数爬虫能兼容，但规范写法是 `name=`。

---

## 七、HTML 语义化结构（页面组件）

爬虫依赖语义化标签理解页面结构，以下是 seedance2.us 的做法：

```html
<!-- main 标签加 id，配合无障碍跳转链接 -->
<main id="main-content" class="pt-16">

<!-- section 加 id，爬虫可以理解各区块用途 -->
<section id="features">
<section id="faq">
<section id="how-to-use">

<!-- footer 加 role 和 schema.org 微数据 -->
<footer role="contentinfo" itemscope itemtype="https://schema.org/WPFooter">

<!-- 品牌区域加 Organization 微数据 -->
<div itemscope itemtype="https://schema.org/Organization">
  <span itemprop="name">站点名</span>
  <p itemprop="description">站点描述</p>
</div>
```

---

## 八、无障碍跳转链接（BaseLayout.astro body）

```html
<body>
  <a href="#main-content" class="sr-only focus:not-sr-only ...">
    Skip to main content
  </a>
  <slot />
</body>
```

这个链接对爬虫有两个好处：
1. 明确告知主内容区域的锚点位置
2. Google 的无障碍评分会影响搜索排名

---

## 九、theme-color 和 format-detection（BaseLayout.astro）

```html
<!-- 移动端浏览器地址栏颜色，提升品牌一致性 -->
<meta name="theme-color" content="#111827">

<!-- 禁止 iOS 自动识别电话号码（避免页面结构被破坏） -->
<meta name="format-detection" content="telephone=no">
```

---

## 优先级排序

| 优先级 | 优化点 | 对爬取的影响 |
|--------|--------|-------------|
| 🔴 高 | robots meta + googlebot/bingbot 指令 | 直接控制爬虫行为 |
| 🔴 高 | robots.txt 声明 sitemap | 爬虫发现所有页面的入口 |
| 🔴 高 | _headers 缓存策略 | 节省 crawl budget |
| 🟡 中 | JSON-LD 补全（publisher/breadcrumb/inLanguage） | 结构化数据质量 |
| 🟡 中 | DNS prefetch + preconnect | 页面加载速度 |
| 🟡 中 | OG/Twitter 标签补全 | 社交分享和爬虫理解 |
| 🟢 低 | 语义化 HTML + 微数据 | 爬虫理解页面结构 |
| 🟢 低 | theme-color / format-detection | 用户体验 |
